

const defaultState = {
    status: "",
    data: null,
    error: null
}

const reducer = (state = defaultState, action) => {
    switch (action.type) {
        case "Case":
            break;

        default:
            return state;
    }
}

export default reducer;